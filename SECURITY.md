# 安全策略

## 支持的版本

`6.3.5` 已冻结待公开。当前实际公开正式版为 [`6.3.1`](https://github.com/LJMcarryu/IFLYADLib_iOS/releases/tag/6.3.1)，继续提供安全更新。

下表是 `6.3.5` 完成正式公开与消费验证后生效的支持策略：

| 版本 | 安全更新 |
| --- | --- |
| 最新公开正式版本（当前为 `6.3.5`） | 正式公开后支持 |
| `6.3.1` 及更早版本 | `6.3.5` 正式公开后建议升级 |

只有不可变 tag、Release 资产与对应 checksum 均可匿名验证的版本才构成公开正式发布；`releaseState=FORMAL` 和 `main` 上已回填的 checksum 本身只证明正式资产元数据已就绪。

### `6.3.5` 冻结证据

正式签名资产和 checksum 已于 2026-09-14 冻结，A/B、候选身份与合并包 SHA-256 见 [RELEASING](RELEASING.md#635-当前发布状态)。10 个资产的匿名下载和消费验证由发布流水线完成后记录。

## 报告安全漏洞

**请勿在公开 Issue / PR / 讨论区中披露安全漏洞**，以免在修复前扩大暴露面。

请通过[讯飞安全应急响应中心](https://security.iflysec.com/front/bugForm)的「提交漏洞/情报」入口私密上报。本仓库当前未开启 GitHub Private Vulnerability Reporting，不要使用公开 Issue 代替私密上报。

上报时请尽量包含：

- 受影响的 SDK 版本与接入方式（CocoaPods / SPM / 手动）；
- 漏洞描述、影响评估与复现步骤；
- 如有，最小化的 PoC。

漏洞的受理、沟通与处置时效以讯飞安全应急响应中心当前公示规则为准，本仓库不另行承诺固定时效。
