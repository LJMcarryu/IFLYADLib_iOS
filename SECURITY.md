# 安全策略

## 6.4.0 支持说明

`6.4.0` 已纳入安全支持矩阵。本版调整 UIScene 展示归属和开屏 `customWindow` 验收，公开 API 方法签名不变；公开状态与消费验证结论以 `release-state.json.publication` 和同版本 GitHub Release 的实际状态为准。

## 支持的版本

`6.3.5` 已完成正式公开、匿名资产校验与消费验证。发布身份和结果见 [发布记录](RELEASING.md#635-冻结与发布记录)。

| 版本 | 安全更新 |
| --- | --- |
| 最新公开正式版本（当前为 `6.4.0`） | 支持 |
| `6.3.1` 及更早版本 | 建议升级到最新正式版本 |

已发布 Tag、Release 和资产不覆盖；需要二进制安全修复时发布新版本。

## 报告安全漏洞

**请勿在公开 Issue / PR / 讨论区中披露安全漏洞**，以免在修复前扩大暴露面。

请通过[讯飞安全应急响应中心](https://security.iflysec.com/front/bugForm)的「提交漏洞/情报」入口私密上报。本仓库当前未开启 GitHub Private Vulnerability Reporting，不要使用公开 Issue 代替私密上报。

上报时请尽量包含：

- 受影响的 SDK 版本与接入方式（CocoaPods / SPM / 手动）；
- 漏洞描述、影响评估与复现步骤；
- 如有，最小化的 PoC。

漏洞的受理、沟通与处置时效以讯飞安全应急响应中心当前公示规则为准，本仓库不另行承诺固定时效。
